# v11 올리는 방법 — 파일 하나씩 복사하지 않습니다

이번엔 파일이 32개라 복사-붙여넣기로는 못 합니다.
대신 **깃허브에 폴더째 끌어다 놓는** 방법을 씁니다. 5분이면 됩니다.

---

## 1단계 — 압축 풀기

받으신 `family-english-app-v11.zip` 을 **압축 해제**하세요.
`v11-upload` 라는 폴더가 생기고, 그 안에 이런 것들이 들어 있습니다.

```
api/          ← 서버 (토큰 발급, 선생님 설정)
web_app/      ← 화면 (3D 아바타, 대화)
package.json
vercel.json
```

> ⚠️ **압축을 꼭 푸세요.** zip 파일 그대로 올리면 안 됩니다.

---

## 2단계 — 새 브랜치 만들기

1. https://github.com/sdonghee/family-english-app 열기
2. 왼쪽 위 **main** 드롭다운 클릭
3. 입력창에 아래를 그대로 입력

```
v11-live-3d
```

4. 아래 파란 글씨 `Create branch: v11-live-3d from 'main'` 클릭

---

## 3단계 — 폴더째 끌어다 놓기

1. 화면이 `v11-live-3d` 브랜치인지 확인하세요 (왼쪽 위)
2. 오른쪽 위 **Add file** → **Upload files** 클릭
3. 아까 압축 푼 `v11-upload` 폴더를 열고,
   **폴더 안의 내용물 전부**(`api`, `web_app`, `package.json`, `vercel.json`)를
   드래그해서 깃허브 화면에 끌어다 놓으세요.
   - `v11-upload` 폴더 자체가 아니라 **그 안의 것들**을 넣어야 합니다.
   - 폴더를 끌어다 놓으면 안의 파일까지 알아서 다 올라갑니다.
4. 파일 목록이 다 뜨면 (30개쯤) 화면 아래 초록색 **Commit changes** 클릭

기존 파일과 같은 이름은 자동으로 새 내용으로 바뀝니다.

---

## 4단계 — Pull Request 만들고 미리보기 확인

브라우저 주소창에 붙여넣고 이동:

```
https://github.com/sdonghee/family-english-app/compare/main...v11-live-3d?expand=1
```

초록색 **Create pull request** → 다음 화면에서 한 번 더 **Create pull request**

1~2분 뒤 PR 화면 아래에 Vercel 이 **Preview** 주소를 답니다.
**그 주소로 먼저 확인하세요.** (지금 쓰시는 주소는 그대로 있습니다)

---

## 5단계 — 미리보기에서 확인할 것

Preview 주소를 휴대폰으로 열고:

- [ ] 프로필 화면에 가족 6명이 다 보이는가
- [ ] 아빠를 누르면 **3D 얼굴**이 뜨고, 눈을 깜빡이고 고개가 미세하게 움직이는가
- [ ] 마이크를 눌러 말하면 선생님이 **말로** 대답하는가
- [ ] 말할 때 **입이 소리에 맞춰 움직이는가**
- [ ] 중간에 멈추지 않고 계속 대화가 되는가

**잘 되면** PR 화면에서 **Merge pull request** 를 누르세요. 그때부터 원래 주소에 반영됩니다.

**안 되면** 아무것도 누르지 마시고, 화면을 캡처해서 저에게 보내주세요.
main 은 그대로라 지금 쓰시던 앱은 멀쩡합니다.

---

## 중요 — 미리 확인해 두실 것

### (1) GEMINI_API_KEY

이미 넣으셨습니다. 이번에도 그대로 씁니다.
혹시 오류가 나면 화면에 **원인이 한국어로 그대로** 뜨도록 만들어 뒀습니다.
(지난번 `GEMINI_API_KE` 오타 같은 건 이제 화면이 알려줍니다)

### (2) Live API 사용 설정

이번 앱은 **실시간 음성(Live API)** 을 씁니다. 지금까지 쓰던 일반 API 와
같은 키를 쓰지만, 계정에서 이 기능이 열려 있어야 합니다.
안 열려 있으면 화면에 그 내용이 한국어로 뜹니다. 그때 알려주세요.

### (3) 요금

실시간 음성은 쓴 시간만큼 요금이 붙습니다.
그래서 프로필마다 **하루 사용 시간 제한**을 걸어 뒀습니다.
(어른 20분, 초등 15분, 6세 12분, 4세 8분 — 설정에서 바꿀 수 있습니다)
화면 위에 오늘 쓴 시간과 대략의 금액이 항상 보입니다.

---

## 만약 v11 이 마음에 안 드시면

옛날 앱을 지우지 않고 같이 넣어 뒀습니다.

```
(주소)/legacy/index.html
```

이 주소로 들어가면 지금까지 쓰시던 그 화면이 그대로 나옵니다.
그러니 안심하고 v11 을 시험해 보셔도 됩니다.
